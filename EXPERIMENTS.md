# 実験ログ：Redis のトレードオフを手で確かめる（Go 版）

各実験は 5 ステップで記録する。**④の一文**が最終成果物。

1. 仮説（何が起きると思うか）
2. ナイーブに動かす（手順・数値）
3. わざと壊す（観察した壊れ方・数値）
4. なぜ？ → 設計思想を**一文**で
5. 直す（対策と、直後の数値）

seeder は `docker compose exec app go run . -seed <件数>` で叩く。

---

## 実験1：速度 ⇔ 耐久性（← まずここから）

**軸**：RAM は速いが揮発する。どこまで失うことを許容するか。

### ① 仮説
- 永続化オフだと、プロセス再起動で全データが消える？

### ② ナイーブに動かす
初期状態の `docker-compose.yml` は `--save "" --appendonly no`（永続化オフ）。

```bash
docker compose up -d
docker compose exec app go run . -seed 10000
# ★注意: DBSIZE は「キーの数」で 1 を返す（全員が1つのZSETに入っているため）。
#   メンバー数を見たいなら ZCARD を使う。
docker compose exec redis redis-cli ZCARD leaderboard:global   # → 10000
docker compose exec redis redis-cli DBSIZE                     # → 1
```

### ③ わざと壊す
Redis だけ再起動して、中身を確認する。

```bash
docker compose restart redis
docker compose exec redis redis-cli ZCARD leaderboard:global   # → ?
```

観察を記録：0になった（例：0 になった＝全消失）

### ④ なぜ？（一文で言語化）
> 記入：永続化を有効にしない限り、プロセスの再起動（電源が切れたとき）で全て消えるからRedisは真実の記録元（SOT）にはせず、その前に置く必要がある。
> （ヒント：RAM は電源が切れれば消える。だから Redis は既定で「速さ」を取り、
> 　永続化は明示的に足すオプション。＝真実の記録元にはしない設計）

### ⑤ 直す ＆ 窓を測る
`docker-compose.yml` の redis command を切り替えて比較する。

- **RDB（スナップショット）**：`redis-server --save "60 1000"`
  → 「60秒ごと（1000変更以上あれば）に丸ごと保存」。クラッシュ時は直近スナップ以降を失う。
- **AOF（追記ログ）**：`redis-server --appendonly yes --appendfsync everysec`
  → 毎秒 fsync。失う窓は最大1秒だが書き込みコストが乗る。

各設定で seed → `docker compose kill redis` → `up` → `ZCARD leaderboard:global` を測り、
「どれだけ残ったか（=失った窓）」を記録：

| 設定 | kill 後に残った件数 | 失った窓 | 体感の書き込み速度 |
|------|------|------|------|
| 永続化オフ | 0 | 全て | 0.079ms |
| RDB save 60 1000 |　2482 | 7518 | 0.079ms |
| AOF everysec |　10000 | 0 | 0.071ms |
| AOF always | 10001 | 0 | 0.559ms |

**この表が「なぜ Redis を source of truth にしないか」の証拠になる。**

### 観察結果①
Redisのデータが消える要因は「永続化なしでのプロセス終了による揮発」と「メモリ上限での意図的な排除」の2つ
永続化を有効にしても消失はゼロにならない。
ROFまたはAOFの設定によって「失う窓」が縮むだけで耐久性は0か1ではなく、性能と引き換えにどれだけ喪失を許容するかを選ぶ連続的なトレードオフである。
だからこそ、Redisは真実の記録元にはせず、その手前に置く速い揮発層として扱う。

### 観察結果②
RDB=点、AOF=線とするなら、非線形であり最大1秒の喪失（AOF everysec）までは書き込み速度をほぼ落とさないが、失った窓を0に近づける（AOF always）だと約8倍遅くなる。だから実務の定番はeverysec（ほぼタダで買える耐久性）である。

### 最終的なまとめ
Redisはデフォルトで揮発し、永続化しなければ再起動で全て消える（永続化なしでのプロセス終了による揮発）
そのため真実の記録元（SOT）にはせず、その手前の層として使う。
ただし、永続化を有効にしたからといって消失がゼロにはならない。
ROBまたはAOFの設定によって「失う窓」が縮むだけで耐久性は0か1ではなく、性能と引き換えにどれだけ喪失を許容するかを選ぶ連続的なトレードオフである。
また、RDBを点・AOFを線とするなら、非線形であることと最大1秒の喪失（AOF everysec）までは書き込み速度をほぼ落とさないが、「失った窓」を0に近づける（AOF always）だと約8倍遅くなる。
実務での定番がeverysecなのは、書き込み速度と失う窓のトレードで最も良いから（ほぼタダで最大1秒まで縮められる）

> 補足：「失う窓」の別表現
RPO（Recovery Point Objective/目標復旧時点）
→ 「障害時にどこまでのデータ喪失を許容するか」を時間で表す指標そのもの
AOF everysec：RPO ≒ 1秒
AOF always：RPO ≒ 0秒
永続化オフ：RPO ≒ 無限大（全部）
DR（災害復旧）や可用性設計では必ず出てくる用語

↔︎ RTO（Recovery Time Objective/復旧までにかかる時間）

RDB：復旧が早い（スナップショットを読むだけ）がRPOが大きい
AOF：RPOが小さいが復旧が遅い（ログ再生がいる）

より別の言い方
data loss window（データ損失ウィンドウ）
durability gap / durability window / exposure window（晒されている時間）

つまり・・・
窓の実態は「まだディスクにflushされていない、メモリ上だけの書き込みの溜まり」
AOFのappendfsync間隔 = 窓の幅 （everysecが最大1秒であること）

---

## 実験2：メモリ ⇔ データ量

**軸**：RAM は高価で有限。全データは載らない。

### ② ナイーブ
`maxmemory` を小さく設定（例 `redis-server --save "" --appendonly no --maxmemory 3mb --maxmemory-policy noeviction`）して
`docker compose exec app go run . -seed 200000` を流す。

### ③ 壊す
- `noeviction` だと：新規書き込みが `OOM command not allowed` で弾かれるのを観察。
- `--maxmemory-policy allkeys-lru` に変えると：古いキーが黙って消えるのを観察。

`redis-cli INFO memory` の `used_memory_human` と `evicted_keys` を記録。

### ④ なぜ？（一文）
> 記入：noevictionはメモリ上限到達時に新規書き込みを拒否してデータを守り、
> allkeys-lruは古いキーを追い出して書き込みを守るが、追い出しはキー単位であるため、
> 全てのデータを単一の巨大なキーに入れると部分的に削ることができず、上限毎にキーごと全て失う。
> （ヒント：「ホットな一部だけ載せる」は制約ではなく前提。だからエビクション方針が存在する）

### 観察結果①
Redisのメモリ上限到達時の振る舞いはポリシーで選ぶ。noevictionは書き込みを拒否してデータを守り、allkeys-lruは古いキーを追い出して書き込みを守る。ただし追い出しはキー単位なので、全データを単一キーに入れていると部分的に削れず、上限のたびにキーごと全損する(20万投入→10回の全損→5073件残存)。ゆえにエビクション設計は実質キー分割設計であり、「何をホットとして残すか」はキーの切り方そのものである。

### 観察結果②
同じallkeys-lru・同じメモリ上限でも、全員を単一キーに入れると上限のたびに全損する一方、read/writeのまとまりでキーを分割すると、LRU（最近使われていないキーから追い出す）は冷たいシャードから最小限に追い出し、ホットなデータを守れた(old:0/old:1が0になる一方hotは生存)。ゆえにエビクション時に「何が生き残るか」はポリシーだけでなくキーの切り方が決める。

### 観察結果③
キーを分割すると、LRUは冷たいシャードから先に追い出すため、ホットなデータは冷たい在庫がある間は守られる(old:0/old:1が先に全損、hotは生存)。ただしこれは容量問題の先送りであり、ワーキングセット自体が上限を超えると冷たい在庫が尽き、最終的にはホットも削られ始める(evicted_keys=3、hotも一度蒸発して7231件で再構築)。ゆえにキー分割はエビクションを「制御可能」にするが、根本解決には容量設計(maxmemoryの確保やTTLでの計画的な期限切れ)が要る。

### 観察結果④
allkeys-lruの挙動はワーキングセットとmaxmemoryの大小で連続的に変わる。maxmeoryに収まるうちはエビクションゼロ（全生存）、超え始めると冷たいキーから追い出してホットを守り（キー分割が効く）、ワーキングセット自体が上限を超えると冷たい在庫が尽きてホットも削られる。
だから、evicted_keysとused_memoryは「キャッシュが健全か」の監視指標になる。

### 最終的なまとめ
Redisのメモリ上限到達時の振る舞いは2つのポリシーを適切に選ぶ必要がある。（代表例としてnoevictionとallkeys-lru）
noevictionに設定するとメモリ上限時に新規書き込みを拒否することでデータを守る。
一方、allkeys-lruは古いキーを追い出して書き込みを守るが、追い出しはキー単位で行うので、全データを単一のキーに入れていると部分的に削るという選択肢が取れず、上限に到達するごとにキーごと全損する。
read/writeのまとまりでキーを分割すると、最後にアクセス（読み取りまたは書き込み）されてからの時間が長いキー（=冷たいキー）から最小限に追い出すことでホットなデータを守る。
しかし、ワーキングセット自体が容量の上限を超えると、追い出せる冷たいキーを使い切り最終的にはホットなキーも削られる。
キー分割自体はエビクションを制御可能にするが、根本の解決を行うためには容量設計(maxmemoryやTTLでの計画的な期限切れ）も行う必要がある。
だから、evicted_keysとused_memoryを観察することで「キャッシュが健全か」の監視指標になる。

---

## 実験3：低レイテンシ ⇔ リッチクエリ

**軸**：クエリプランナも二次索引もない。アクセスパターン先行でキーを設計する。
RedisにはSQLのWHEREやJOIN、二次索引がない。条件付きのデータを取得するには単一ZSETでは取得できない？
現状のデータ（メンバ）には国の情報がどこにもないので、特定の国だけで絞る情報がゼロ。

### ③ 壊す
「スコア100超のユーザーを国別に」みたいな条件を出そうとして、ZSET 1本では表現できないことに気づく。
`KEYS *` や全 `SCAN` で無理やり走査 → 遅い＆重いのを `redis-cli --latency` で観察。

### ④ なぜ？（一文）
> 記入：Redisには実行時にどう探すのが速いかを考えないため、条件付きのデータを取得するためには設計時にアクセスパターンを固定して（先にどう読むかを決めて）から、専用のキー構造を先に用意する。
> （ヒント：先に「どう読むか」を決めてキーを設計する。例：`leaderboard:jp` を別 ZSET で持つ）

### ⑤ 直す
Go 側で国別キーに二重に `ZADD`（`AddScore` を国別キーにも打つ）など、
データの持ち方で解く設計に。`leaderboard.go` に `AddScoreTo(ctx, key, ...)` を足すのが素直。

### 観測結果① Redisのキー設計とトレードオフ
Redisにはクエリプランナも二次索引もJOINも無いので、「日本の上位10人」のような条件付き取得を単一のグローバルZSETで行うと、全件スキャン(O(N))してアプリ側で絞るしかなく、10万件で約7msかかった。一方、読み方に合わせて国別キーを先に用意すれば、上位10件はO(log N)で数µs。しかも全件スキャンは件数に比例して悪化するのに対し、専用キーは件数にほぼ不感なので、データが増えるほど差は開く。ゆえにRedisでは「どう読むか」を設計時に固定し、その読み方専用のキー構造を先に作る——柔軟なクエリを捨てて予測可能な速度を買うのがトレードオフ。

### 観察結果② スコア更新時に二重書き込みを実装（globalと国別キーへの両方へ書く）
二重書き込みはその経路を通った書き込みしか反映されないため、途中から対応を追加した時（国別の対応を足したとき）には既存データのバックフィル（遡って国別キーを埋める処理が必要）

### 最終的なまとめ
Redisにはクエリプランナも二次インデックスもJOINも無いので、単一のグローバルZSETに全ユーザーを入れたまま「特定の国の上位10人」を出そうとすると、メンバーに国の情報がないため全件をスキャンしてアプリ側で絞る必要がある。しかもこのスキャンは件数に正比例して悪化し、件数を1000→20万に増やすと343µs→133,603µsと伸びる一方、専用キーでの上位取得は11→16µsとほぼ動かず、差は約8,000倍まで開いた。
だからRedisでは「どう読むかを先に決め、その読み方専用のキー構造を先に用意する」。国別に読むなら最初から国別にキーを持たせる。そうするとクエリは「該当キーを名指しで引く」に変わり、国別上位はO(log N)となる。キーの切り方がそのまま「どんな問い合わせが速いか」を決めており、これが「キー設計=クエリ設計」。
ただし読み方を固定する代償として、柔軟性の放棄(設計時に想定しなかった軸では引けない)と、スコア更新のたびに全体キーと国別キーへ二重に書くコスト(書き込み2倍・データ二重・整合性を自前で保つ責任)がある。しかしこのコストは弱点ではなくトレードオフの表裏で、全件スキャンがO(N)で悪化するのに対し専用キーの取得はO(log N)なので、前払いした設計コストは規模が大きくなるほど大きな見返りになる。
RDBが「まず正規化して、読み方は後から考える」のに対し、Redisは「まず読み方を決め、それに合わせてデータを配置する」。柔軟性と引き換えに予測可能な低レイテンシを買う、その判断の最前線がキー設計である。

---

## 実験4：可用性 ⇔ 強整合性（レプリケーション）

**軸**：非同期レプリのため、フェイルオーバー時に ACK 済み書き込みを失いうる。

### 準備
`docker-compose.yml` に replica を1台追加：

```yaml
  redis-replica:
    image: redis:7-alpine
    command: redis-server --replicaof redis 6379
    depends_on: [redis]
```

### ③ 壊す
プライマリに書き込み → 即 `docker compose kill redis` → replica を昇格させ、
直前の書き込みが replica に伝播していない（消えている）ケースを再現。

### ④ なぜ？（一文）
> 記入：全レプリカの確認を待つことなく成功を返すことで、性能・可用性をとり、強整合性を諦める。
> （ヒント：整合性より可用性・性能に倒す判断。CAP 的な割り切り）

### まとめ
Redisのレプリケーションは既定で非同期のため、プライマリは全レプリカの確認を待つことなくクライアントへ成功を返す。これは性能と可用性優先の選択で、代償としてフェイルオーバー時にプライマリがACKした際にはレプリカに未伝播だった書き込みを失う可能性。これは可用性をとり強整合性を諦める。緩和策のWAITで伝播を待てば失う窓を縮めることができるが、待つ分だけ遅くなる。
だからこそRedisは整合性が絶対のSOTにはせず、速い層として使い、整合性はWAITやDB側で必要なだけ追加する。

> 補足：用語
レプリケーション(replication / 複製)
同じデータを複数のRedisサーバーに持たせること。1台が壊れても他が肩代わりできる(可用性)、読み取りを複数台に分散できる(負荷分散)、といった目的があります。redis-replica を足して、プライマリの書き込みが自動でレプリカにコピーされる全体がレプリケーションです。

プライマリ / レプリカ(primary / replica)
レプリケーションでの役割分担。プライマリが書き込みを受け付ける本番機、レプリカがその複製を受け取る控え機。書き込みは必ずプライマリに行き、レプリカはそれを追いかけてコピーする、という一方向の関係です(旧称でmaster/slaveとも。今は primary/replica が推奨表記)。

非同期レプリケーション(asynchronous replication)
これが実験4の主役概念です。「非同期」とは、プライマリがレプリカの受け取り確認を待たずに、クライアントへ先に成功を返す方式。書き込む→即OKを返す→(その後こっそり)レプリカへ送る、という順序です。対義語は同期(synchronous)レプリケーションで、こちらは全レプリカが受け取るまでOKを返しません。Redisは既定で非同期を選んでいる——だから速いが、返した後・伝播する前にプライマリが死ぬと、その書き込みが宙に浮いて消える。

フェイルオーバー(failover)
プライマリが落ちたとき、レプリカを新しいプライマリに昇格(promote)させて運用を継続すること。「障害(fail)を乗り越える(over)」の意。replicaof no one(誰の複製でもなくなる=独立したプライマリになる)を打った操作が、手動フェイルオーバーそのものです。本番では Redis Sentinel や Cluster がこれを自動でやります。

ACK(acknowledgment / 確認応答)
「受け取った/成功した」という応答のこと。文脈で2種類あり、ここが混同しやすい点です。実験4で問題になったのは「プライマリがクライアントに返すACK」——書き込み成功の返事。一方、同期レプリケーションが待つのは「レプリカがプライマリに返すACK」——複製を受け取った返事。非同期の罠は、前者(クライアントへのACK)を返した時点で後者(レプリカのACK)がまだ来ていないこと。「成功と言ったのに複製されていない」というズレの正体です。

WAIT
非同期の隙間を埋めるためのコマンド。WAIT numreplicas timeout は「指定台数のレプリカに伝播するまで(最大timeoutミリ秒)待て」という命令。書き込み直後に WAIT 1 100 を挟むと、「1台に複製されたのを確認してから次へ進む」＝擬似的に同期寄りにできる。ただし待つぶん遅くなり、timeoutすれば保証なしで進む(戻り値が要求台数に満たない)。「同期を強制できるが完全な保証ではない」半端さも含めて、WAIT は速度と整合性のダイヤルを手元に持つ手段です。

CAP定理(CAP theorem)
分散システムの根本原理。Consistency(整合性:どのノードを読んでも同じ最新の値)、Availability(可用性:常に応答が返る)、Partition tolerance(分断耐性:ノード間の通信が途切れても動き続ける)の3つは、同時に全部は満たせないという定理です。ネットワーク分断(P)は現実に起きるので選べず、実質「分断が起きたとき、CとAのどちらを取るか」の選択になります。RedisのレプリケーションはAを取る側——分断時も応答を返し続け、その代わり整合性(最新性)を諦める。あなたが実験4で切り離し→書き込み→昇格でやったのは、まさに「分断が起きた状況でRedisがどう振る舞うか」を人工的に作る操作で、結果として整合性が破れた(lostuser消失)わけです。


---

## 実験5：シングルスレッド ⇔ マルチコア

**軸**：ロック競合ゼロで単純・速いが、1コアで捌く。重いコマンド1本で全員が詰まる。

### ③ 壊す
別ターミナルで `redis-cli --latency` を回しながら、
巨大 ZSET に対して `ZRANGE leaderboard:global 0 -1 WITHSCORES` や `KEYS *` を1本投げる。
その瞬間 latency が跳ねる（head-of-line blocking）のを観察。

Go 側から負荷をかけるなら、goroutine を多数立てて同時に `POST /leaderboard/score` を叩き、
その最中に重いコマンドを差し込むと、クライアント並行性を上げても Redis 側が
1スレッドで直列化する様子が見える。

### ④ なぜ？（一文）
> 記入：シングルスレッドはロック競合ゼロかつ決定的で速いが、1本の重いコマンドが後続を全て止めるため本番ではKEYS*や巨大なZRANGE 0 -1は禁忌で、SCANやZSCANで分割して読む。
> （ヒント：1本の重いコマンドが後続を全部止める。だから本番で KEYS は禁忌、SCAN を使う）

---

## まとめ（全実験を終えてから書く）
Redisの本質は、DBのクリティカルパスからホットなデータを外へ逃す「速い層」であり、一貫して「速さ・可用性」を選び「耐久性・整合性・クエリの自由・並列性」を連続的に手放すことで実現。手放す量は0か1ではなくダイヤルで選べる。

耐久性：RAMは揮発し、永続化しなければ再起動で全て消える。永続化を有効にしたからといって消失はゼロにならず、RDBまたはAOFの設定で「失う窓（RPO）」が縮むだけである。
またRDBとAOFの関係性は非線形であり、最大1秒（AOF everysec）までは書き込み速度をほぼ落とさないが、「失った窓（AOF always）」をゼロに近づけると約8倍遅くなる。実務での定番がeverysecなのは、書き込み速度と失う窓のトレードで最も良いからである。
だから真実の記録元（SOT）にはせず、その手前に置くことが前提。

メモリ：RAMは有限で全データは乗らないため、メモリ上限到達時の振る舞いを適切に選ぶ必要がある。
代表例としてはnoevictionとallkey-lru。
noevictionに設定すると、メモリ上限に到達した時に新規書き込みを拒否することでデータを守る。
一方、allkeys-lruは古いキーを追い出して書き込みを守るが、追い出す時はキー単位のため全データを単一のキーに入れていると部分的に削るという選択肢が取れず、上限に到達するごとにキーごと全損する。
ここで、read/writeのまとまりでキーを分割すると、最後に読み取りまたは書き込みされてからの時間が長いキー（=冷たいキー）から最小限に追い出すことで、ホットなデータを守る。
しかし、ワーキングセット自体が容量の上限を超えると、追い出せる冷たいキーを使い切り、最終的にはホットなキーも削られる。
キー分割自体はエビクションを制御可能にするが、根本の解決にはmaxmemoryやTTLでの計画的な期限切れといった容量設計が必要。
そのためevicted_keyとused_memoryを観察することが、「キャッシュが健全か」の監視指標となる。

クエリ：Redisにはクエリプランナ・二次索引・JOINがない。条件付き取得を単一キーで行うと全件スキャン（O(N))となり悪化する。だから「どう読むかを先に決め、その読み方専用のキーを用意」すれば、クエリは「該当キーを名指しで引く」に変わり、（O(log N)）。キーの切り方がそのまま「どんな問い合わせが速いか」を決めており、これがそのまま「キー設計=クエリ設計」になる。
ただ読み方を固定する代償として、柔軟性の放棄とスコア更新のたびに全体キーと個別キーへ二重に書くコストがある。
しかし、このコストはトレードオフの表裏で、全件スキャンがO(N)で悪化するのに対し、専用のキー取得は0(log N)なので、前払いした設計コストは規模が大きくなるほど大きな見返りになる。
RDBが「まず正規化して、読み方は後から考える」のに対し、Redisは「まず読み方を決め、それに合わせてデータを配置する」。

整合性：Redisのレプリケーションは既定で非同期のため、プライマリは全レプリカのACK（確認応答）を待つことなく成功を返す。そのため、フェイルオーバー（レプリカを新しいプライマリに昇格させる）とき、ACK済みの書き込みを失う可能性になる。緩和策のWAITで伝播を待てば失う窓を縮めることはできるが、待つ分だけ遅くなる。
これは目標復旧時点（RPO）と同じ構造となる。

並列性：シングルスレッドはロック競合ゼロかつ決定的に速いが、1本の重いコマンドが後続全てを止める。
だから本番ではKEYS*や巨大なZRANGE 0 -1は禁忌で、SCANやZSCANで分割して読むのが原則。
1コアの限界はインスタンスの分割やClusterで超える。

上記5つは別々ではなく、「速さと引き換えにどれだけの喪失・不自由を許容するかを具体的なダイヤルで選ぶ」という一つの思想の5つの側面である。そしてその5つのダイヤルが守っている唯一の一点が、「予測可能な速さ」。Redisが絶対に譲らないのは「速い」ことそのものではなく、「いつ叩いても同じ速さで返る」という予測可能性——上位取得は件数が増えてもO(log N)で数µs、その保証を守るために、耐久性も整合性もクエリの自由も並列性も可変オプションへ格下げした。だからRedisを使うとは、この"予測可能な速さ"という一点を手に入れる代わりに手放したものを、RPO・maxmemory・キー設計・WAIT・コマンド選択というダイヤルで、自分のアプリに必要なぶんだけ意識的に買い戻す設計判断に他ならない。

> Redis の核心を、実験で得た数値を添えて自分の言葉で1段落に：
5つに共通する主語：「RedisはXを固定する代わりにYを予測可能にしている。」
→ Redisが一貫しているのは単なる速さではなく、予測可能な速さ（いつ叩いてもO（log N）で数μs）という保証

Redisが絶対に譲らないもの：1コマンドの実行が速く、かつ予測可能であること。
これを守るためにRedisは、そのほかの全て（耐久性・整合性・柔軟性・並列性）を差し出した。

例え：Redisは全部入りの金庫ではなく、手元に置く作業台（狭くて物が落ちる=揮発、エビクション）。
DBは遅いかわりに何でも安全にしまえる。
Redisは置き方を決めないと探せないが手が届く速さがある。

Redisの核心は「予測可能な速さ」を唯一の絶対条件とし、それ以外の全てを「必要な分だけ買い足す可変オプション」にしたこと。DBが「まず全部を正しく保存し、速さは後から足す」なら、Redisは「まず一定の速さを保証し、正しさや便利さは後から足す」という逆転の発想となる。だからRedisは万能の記録装置ではなく、「予測可能な速さ」という一点だけを尖らせた道具であり、Redisを使いこなすとは、周りで手放されたものを自分のアプリに必要なだけ意識的に買い戻す設計判断になる。

※逆に考えるなら「予測可能性がいらない場面はRedisは不要」という使わない判断の基準になる。
例えば「失えない基幹データ（耐久性が絶対）」や「複雑なアドホック分析（クエリの自由が絶対）」ならRedisが譲ったものこそが要件になるので、Redisを使うべきでない。


## 実験ログ 発展A：大量の同時アクセスを捌く（飽和点とテールレイテンシ）
### この環境での実測（heavyなし）
```
並行度      rps          p50(ms)   p99(ms)   p999(ms)  max(ms)  
--------------------------------------------------------------
1        29484        0.03      0.05      0.09      1.31     
8        108955       0.07      0.16      0.29      2.39     
32       302356       0.10      0.27      0.76      10.07    
128      325389       0.37      1.02      1.98      46.87    
512      307438       1.58      3.22      16.79     149.98  
```
読みどころ：
①rpsは並行度32で30万rpsまでで頭打ちとなり以降増やしても伸びない。1コアだからクライアントを増やしても伸びない。
②p50は並行度に比例して伸びる（0.03→1.58ms）
③テールが最後に膨らむ（max1.31→149.98ms）
　飽和点を超えて同時接続を増やすと、スループットは増えずレイテンシだけ増える

### この環境での実測（heavyあり）
※重いコマンドを差し込んだ場合
```
並行度      rps          p50(ms)   p99(ms)   p999(ms)  max(ms)  
--------------------------------------------------------------
1        29256        0.03      0.05      0.08      5.03     
8        108447       0.07      0.15      0.25      3.82     
32       292527       0.10      0.29      1.06      6.14     
128      313058       0.37      1.11      3.61      91.97    
512      304178       1.59      4.39      13.23     132.89  
```

### 観察結果
飽和点（スループットが頭打ちになる並行度）はマシン性能で変わるが、「飽和して以降はスループットが伸びずテールだけ悪化する」という現象自体は、単一スレッドの構造から来る不変の性質
また、head-of-line blocking（巨大なZRANGEを投げ、その間他の全クライアントが固まる）の被害の大きさは、重いコマンドの実行時間と頻度に比例するため、マシンが速く重いコマンドが軽ければ、テールに埋もれて見えないこともある。だから監視するべきは平均ではなくp99/p999であり、かつ「自分の環境でどの操作がどれだけ重いか」を実測して初めて危険が見える

### 追加観察：100万件にして重いコマンドを重くする。かつ差し込み感覚を速くする
```
docker compose exec app go run ./cmd/load -op read -c 8,128 -d 3s
並行度      rps          p50(ms)   p99(ms)   p999(ms)  max(ms)  
--------------------------------------------------------------
8        110597       0.07      0.16      0.22      1.79     
128      323029       0.37      1.03      2.02      74.68
```
```
docker compose exec app go run ./cmd/load -op read -c 8,128 -d 3s -heavy -heavy-every 200ms
並行度      rps          p50(ms)   p99(ms)   p999(ms)  max(ms)  
--------------------------------------------------------------
8        49381        0.07      0.17      1.23      122.67   
128      152305       0.38      1.08      111.62    125.07
```
読みどころ：
** heavyありでrpsが半減しテールが跳ねる。**
並行度8でrpsが110,597 → 49,381（約半減）、並行度128でrpsが323,029 → 152,305（約半減）。
テールは並行度8のmaxが1.79ms → 122.67ms、並行度128で2.02ms  → 111.62ms

この計測環境は速いことで、10万件では重いコマンドが軽すぎて埋もれ、100万件にしたことで初めて(123ms)顕在化した。同じコードでもデータ量とマシン性能の掛け算で、危険が見える閾値が動く。
⇨ 「head-of-line blockingの被害は重いコマンドの実行時間に比例する」の実証（速いマシンは危険を遅らせるが、消すことはない）

### まとめ
head-of-line blocking（巨大なZRANGEを投げ、その間他の全クライアントが固まる）の被害の深さは、「重いコマンドの実行時間」と「その頻度（差し込み間隔）の比で決まる。重いコマンドが間隔より長ければスレッドをほぼ占有し、rpsは大崩落し、短ければ空き時間に軽い処理が捌けてrpsは部分的に保たれる。いずれにせよテールは跳ねる。重いコマンドの実行時間はデータ量とマシン性能の積で変わるため、危険が顕在化する閾値は環境ごとに動く
そのため監視するべきはp99/p999であり、かつ自分の環境・自分のデータ規模で実測しない限り、この罠はわからない。

## 実験ログ 発展B：水平スケール（レプリカでの読み分散）
ここまで：書き込みはプライマリ1台に集約する必要があるが、読み取りは全レプリカにコピーがあるので、複数台に振り分けられる。読み負荷が支配的なワークロードなら、レプリカを足すほど読みのスループットが伸びる。

落とし穴：レプリケーションは非同期（実験4）。だからレプリカから読むとプライマリには反映済みだが、レプリカにはまだ届いていない古い値を読むことがある（replication lag / 読み取り整合性の問題）
「スコアを更新した直後に自分の順位を見たら古いままだった」という現象
読み分散は整合性をさらに緩める代わりにスループットを買う（実験4のトレードオフが読み側で再登場する）

### 観察結果①
**レプリカは書き込みを拒否する**
これが読み分散の大前提。書き込みはプライマリ1台に集約するしかなく、水平スケールしても書き込みのボトルネックは残る。「読みは分散できるが、書き込みは1点集中」という非対称がシャーディングの動機。

### 観察結果②
**読みはレプリカでも同じ速さで捌ける=台数分読み容量が増える**
実測はプライマリ289,603rps・レプリカ298,991rpsでほぼ同等。読みをレプリカに逃せばプライマリは書き込みに専念でき、読み容量は台数分増える。これが実験Aにおける「単一ノードの飽和点」を読みについて水平に越える方法

### 観察結果③
代償として、レプリカの読みは古いことがある（replication lag）。
複製は非同期のため、書き込みの負荷が高いとレプリカはプライマリに追いつけず遅れる。
localhostで数MB遅れる瞬間があるので、実ネットワーク（クロスAZ/リージョン）ではもっと大きくなる。
その遅れの間、レプリカを読むと「スコアを更新した直後に自分の順位を見たら古いまま」が起きる。

### まとめ
読み取りは全レプリカにコピーがあるので水平分散できる。ランキング表示などの読み負荷が高いワークロードではレプリカを足すほど読み容量は増える。ただし、複製は非同期のため、レプリカの読みは古いことがある。
読み分散は、「整合性をさらに緩める代わりに読むスループットを買う」トレードオフとなる。
強い最新性が必要な読み（スコア直後の確認）はプライマリ、緩くて良い読み（他人のランキング）はレプリカからと、読みを二層に分けるのが定石。

## 実験ログ 発展B：水平スケール（Redis Clusterによるシャーディング）
Clusterの核心は**ハッシュスロット**
Redis Clusterは16384個の「スロット」を用意し、各キーをCRC16(key) % 16384でスロットに割り当て、スロット範囲を複数のプライマリで分担。キー名から機械的に配置ノードが決まるので、キーの切り方は重要。

制約①：マルチキー操作の制約
単一ノードならTxpipelineで1トランザクションにできるが、Clusterでは別々のノードに載っていたら1つのトランザクションにはできない。(CROSSSLOTエラー)。複数キーをまたぐ操作は同じスロットがある時しか許されない。

制約②：ハッシュタグ（制約①の解決策）
キー名の{...}で囲った部分だけをハッシュ計算に使う仕組み。{user}:globalと{user}:jpのように書くと、{user}部分が同じなので**必ず同じスロット=同じノード**に載る。キー設計がClusterでは「どのキーを同居させるか」というスロット設計に進化する。

Clusterの起動
```bash
docker compose -f cluster/docker-compose.yml up -d       # 6ノード起動+init が自動組成
docker compose -f cluster/docker-compose.yml exec redis-node-1 redis-cli -p 6379 cluster info
```

> 補足：分散システムのタイミング
分散システムは「起動した瞬間」と「使える瞬間」がずれる。
単一ノードのRedisは起動したら即使用可能だったが、Cluserは全ノードがゴシップ通信で互いの状態を伝播し、全員が「全スロット健全」になってから初めて使用可能になる。本番の自動化（composeのinitやヘルスチェック）で「クラスタがokになるまで待つ」処理が必要なのはこのため。

```bash
# ① キーはスロットで別ノードに分散する
docker compose -f cluster/docker-compose.yml exec redis-node-1 redis-cli -p 6379 cluster keyslot leaderboard:jp
(integer) 3855

docker compose -f cluster/docker-compose.yml exec redis-node-1 redis-cli -p 6379 cluster keyslot leaderboard:us
(integer) 11297

# ② マルチキー操作の制約：別スロットをまたぐと CROSSSLOT
docker compose -f cluster/docker-compose.yml exec redis-node-1 redis-cli -c -p 6379 mset leaderboard:global 1 leaderboard:jp 2
(error) CROSSSLOT Keys in request don't hash to the same slot

# ③ ハッシュタグで同居 → OK
docker compose -f cluster/docker-compose.yml exec redis-node-1 redis-cli -c -p 6379 mset "{u1}:global" 1 "{u1}:jp" 2
OK

# ★決定的な結論：単一キーは1ノードにしか載らない
docker compose -f cluster/docker-compose.yml exec redis-node-1 redis-cli -c -p 6379 zadd leaderboard:global 100 alice
(integer) 1

docker compose -f cluster/docker-compose.yml exec redis-node-1 redis-cli -p 6379 cluster keyslot leaderboard:global
(integer) 5355
```

### 観察結果
leaderboard:globalとleaderboard:jpは別スロットなので、二重書き込みがCROSSSLOTで壊れる。ところが、{u1}:globalと{u1}:jpはハッシュタグ{u1}が同じなので、同一スロットに固定されOKとなる。キーの置き方が、操作の可否そのものを決めている。
また、leaderboard:globalはslot 5355に固定（=1ノードにしか載らない）ので、1つのZSETはシャーディングされない。ここから、グローバルランキングがCluster上でも1ノードの限界に縛られる理由。

### 一言でまとめる
**Redisの設計思想は「キーをどう切るか」**

### 最終結論
実験②：単一キーは丸ごと蒸発する
実験③：キー設計=クエリ設計
実験⑤：1キー=1スレッドが占有される
実験B②：「1キー=1スロット=1ノードで、キーの切り方が分散も原子性も決める」
⇨ 同じ一つの原理が、耐久性・メモリ・クエリ・並列性・分散という5つ全て貫いていた。

Redisの核心は「予測可能な速さ」を唯一の絶対条件とし、それ以外の全てを「必要な分だけ買い足す可変オプション」にしたこと。
その可変オプションを操作する唯一の物理的な手綱が「キーをどう切るか」。
キーは、「消える単位(エビクション)」であり、「引ける単位(クエリ)」であり、「詰まる単位(シングルスレッド)」であり、「載るノードを決める単位(スロット)」である。
そのため、Redisを設計するとは、アクセスパターンに合わせてキーをどう切るか決めることである。
それは単一ノードでも分散でも変わらない一貫した設計思想である。
DBが「まず全部正しく保存し、速さは後から足す」なら、Redisは「まず一定の速さを保証し、正しさや便利さは後から足す」。
だからRedisを使いこなすとは、その周りで手放されたものを「キーの切り方」を通じて必要な分だけ意識的に買い戻す作業である。